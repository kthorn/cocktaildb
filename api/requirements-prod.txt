# api/requirements-prod.txt
# Production-only dependencies (EC2 with PostgreSQL)

# PostgreSQL adapter
psycopg2-binary==2.9.9

# ASGI server
uvicorn[standard]==0.24.0

# Process manager (optional, for gunicorn)
gunicorn==21.2.0
