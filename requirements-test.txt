# Testing dependencies
# Install with: pip install -r requirements-test.txt
#
# This file extends the main requirements.txt with test-specific dependencies

-r api/requirements.txt

# Test frameworks (some already in requirements.txt, listed here for clarity)
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-mock>=3.14.1
pytest-cov>=4.1.0
httpx>=0.24.0

# Lint. Pinned to the same version as the ruff-check pre-commit hook and as
# required-version in ruff.toml, so a CI step installing from this file cannot
# run a different, wider rule set.
ruff==0.16.5

# Focused database typing regression check
pyright==1.1.408

# PostgreSQL testing with Docker containers
testcontainers>=4.0.0

# PostgreSQL driver (should already be installed via psycopg2)
psycopg2-binary>=2.9.0

# Local deployment harness (Python 3.12+, matching the API runtime).
# The full Ansible distribution includes collections used by deploy.yml.
ansible==13.1.0
PyYAML==6.0.2
